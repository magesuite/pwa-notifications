stages:
    - codesniffer

default:
    image: composer:latest

phpcs:
    stage: codesniffer
    script:
        - mkdir phpcs
        - cd phpcs && composer require squizlabs/php_codesniffer
        - phpcs/vendor/bin/phpcs --standard=PSR12 --ignore=phpcs/ ./
